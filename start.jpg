import random

images = ["start1.jpg", "start2.jpg"]  # Local files only
await message.reply_photo(
    photo=open(random.choice(images), "rb"),
    caption="Welcome text",
    reply_markup=buttons
)
